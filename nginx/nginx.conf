events {}

http {
    upstream service_api {
        server service-api:3002;
    }

    upstream core_api {
        server core-api:8080;
    }

    server {
        listen 80;

        location /api/core/ {
            proxy_pass http://core_api;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        location /api/service/ {
            proxy_pass http://service_api;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}